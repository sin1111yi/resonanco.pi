/**
 * Resonanco — Extension entry point bridge
 *
 * Re-exports from src/index.ts so that pi auto-discovery
 * (looking for extensions/*/index.ts) can find the extension.
 */
export { default } from "./src/index.ts";
